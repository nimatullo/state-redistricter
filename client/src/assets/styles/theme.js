import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      main: "#111825",
      white: "#F0F4F9",
      secondary: "#434A55",
    },
  },
});

export default theme;
